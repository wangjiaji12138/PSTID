# ============================================================
# PSTID 项目依赖
# ============================================================

# --- 核心框架 ---
torch>=1.10.0
numpy>=1.20.0
pandas>=1.3.0
scipy>=1.7.0

# --- 机器学习/数据处理 ---
scikit-learn>=0.24.0          # TSNE, PCA, StandardScaler, r2_score, explained_variance_score
tslearn>=0.5.0               # TimeSeriesKMeans, KShape (用于时间序列聚类)
fastdtw>=0.3.0               # 动态时间规整 (DTW)
geopy>=2.2.0                 # 地理距离计算

# --- 可视化 ---
matplotlib>=3.3.0            # 绘图
seaborn>=0.11.0              # 统计可视化 (可选，用于更好的配色)

# --- 训练相关 ---
torchinfo>=1.8.0             # 模型信息统计
tqdm>=4.62.0                 # 进度条

# --- 超参数调优 (可选) ---
# ray[tune]>=2.0.0           # 超参数搜索 (如需调参功能)

# --- 配置文件 ---
pyyaml>=5.4.0                # YAML 配置解析
